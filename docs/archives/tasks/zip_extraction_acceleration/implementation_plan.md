# ZIP展開およびシステムスキャンの高速化

大量のカード画像を扱う際のパフォーマンス（ZIP展開およびフォルダスキャン）を劇的に短縮するための最適化を行いました。

## 変更点
1. **ZIP展開の高速化**:
   - 24並列のワーカープールによる同時書き込み。
   - ディレクトリハンドルのPromiseキャッシュによるOS API呼び出しの削減。
2. **システムスキャンの高速化**:
   - フォルダ探索とファイル取得（getFile）の並列化（同時実行数40）。
   - リンク解決（_link.txt）をMapによる高速検索（O(1)）に変更。
