# 修正内容の確認: カード表示とパス管理の改善

## 1. 動作確認
- [x] **フォルダ表示**: `hololive/deck_example` が正常に表示され、中のカードが確認できること。
- [x] **フォルダ切り替え**: 全く別のフォルダを開いた際、前のフォルダのピン留めカードが消え、新しいフォルダのルート階層から始まること。
- [x] **パスの正確性**: 大文字を含むファイル名（例: `Sui-chan.png`）に対して、`_meta_card.json` で定義した名前や読み（`yomi`）が正しく適用されていること。
- [x] **リンクカードの一致**: 同じ画像を複数の `_link.txt` から参照しても、ピン留めするとすべての場所でピン留め状態が同期されること。

## 2. 副次的な効果
- ファイルシステムを小文字化せずに扱うようになったことで、将来的に大文字小文字を区別する環境（Linux等）へ移植する際や、リモートリポジトリとの同期時のトラブルリスクが低減されました。
- フォルダ履歴管理（上位5件のキャッシュ）と、新規フォルダのクリーン読み込みが矛盾なく両立されました。
